# macOS / editors
.DS_Store
.idea/
.vscode/
*.swp

# Go
/bin/
*.test
*.out
coverage.*

# Bazel symlinks + per-build outputs (at any depth)
bazel-*
/testdata/workspace/bazel-*
**/command.profile.gz
**/.bazel-broker-bep.json
*.profile.gz

# Local broker state / scratch
*.db
*.log
broker.port
broker.token
broker.json

# macOS menu-bar app (E8): generated Xcode project + build output.
# project.yml is the source of truth; regenerate with `xcodegen generate`.
/apps/MenuBar/BrokerMenuBar.xcodeproj/
/apps/MenuBar/build/
/apps/MenuBar/DerivedData/
/apps/MenuBar/docs/screen-*.png

# Stray throwaway harness binaries
*harness

# Per-worktree broker BEP/profile output dir
**/.bazel-broker/
